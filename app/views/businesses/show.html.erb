<h2>Business Details</h2>

<div id="show_business">
	<%= render :partial => "ajax_response" %>
	<%= render 	:partial => "business_box", 
				:locals => {:business => @business, :member => @member,
					 		:owner => @owner, :logged_in => @logged_in
							}%> 
</div>	